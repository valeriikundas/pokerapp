import { makeStyles } from "@material-ui/core"

const useStyles = makeStyles({
  wrapper: {
    display: "flex",
    alignItems: "center",
    flexWrap: "wrap",
  },

  buttons: {
    width: "400px",
    display: "flex",
    alignItems: "center",
    marginRight: "35px",
  },
})

export default useStyles
